---
feature: 2.7
keyword: cache
title: cache sobre o ledger — o ledger arbitra o frescor por sha256, results.json em paralelo
state: confirmed
requirements:
  - `cacheLedger.js` — modulo novo, `openCacheLedger(root, opts)`, expondo a MESMA forma que o objeto `results` interno do `cache.js` ja expunha (`get`/`fresh`/`record`/`flush`) mais um `enabled`. E a igualdade de forma que deixa o `TestCache` trocar um arbitro pelo outro sem saber a diferenca
  - PROJECAO, NAO SEGUNDO REGISTRO: o indice e derivado da stream na ABERTURA, uma passada so — os `run:start` dao o sha256 de cada arquivo do conjunto, os `run:tests` dao o veredito de cada teste, o mais recente vence. O estado presente e derivado do historico, nunca mantido em paralelo a ele
  - ANCORAGEM NO CONTEUDO: `fresh(phase, p, extraDeps, targetPath)` compara o `sha256` do teste, do alvo pareado e de cada dep extra contra o que o ledger registrou — nao o `mtime`. Mesma assinatura de `results.fresh`, para caber no `arbitrate` existente sem reescreve-lo
  - `TestCache(root, { ledger })` — segundo parametro OPCIONAL. Com `ledger?.enabled`, ele arbitra; senao o `results.json`, como sempre. `TestCache(root)` sem opts continua identico byte por byte. Um `judge` unico: o `arbitrate` nao sabe qual dos dois esta respondendo
  - AS DUAS PERSISTENCIAS EM PARALELO: `results.record` roda SEMPRE, qualquer que seja o arbitro. O `results.json` continua sendo o indice que `utest <N.F>` resolve sem escanear e o dono do `failLines` do `-v:2`. Trocar o arbitro nao troca quem escreve — parar de escrever e a convergencia, sprint futuro
  - GRANULARIDADE DO REGISTRO SEGUE A PERGUNTA, NAO O LACO: o sha256 mora no evento AGREGADO (`run:start`), uma vez por rodada, e os `test:result` sao bufferizados num `run:tests` escrito uma vez no `end()`. Hashear ou escrever por teste estoura o `Lock timeout` do iodb com os workers em paralelo — o runner morrendo pelo proprio log
  - O GANHO, E A UNICA DIVERGENCIA ESPERADA: `touch` sem editar e `git checkout` que devolve os mesmos bytes com mtime novo sao `fresh` no ledger e stale no `results.json`. Nos demais casos os dois concordam. `cacheLedger.t.js` fixa essa divergencia como teste, porque e ela que justifica a convergencia
  - CUSTO ZERO QUANDO DESLIGADO: sem `../iodb`, ou desligado por config, `enabled` volta `false" e o `results.json` arbitra — o comportamento de sempre. Mesma lei do 8.1
  - criterio: `utest .` verde no `~/utest`; quente e frio reportam o MESMO numero de checks com o ledger arbitrando (o portao da frente 2); `.utest/results.json` continua existindo e atualizado depois de cada rodada; editar um arquivo derruba `fresh()` nos dois arbitros; `touch` sem editar NAO faz o arquivo re-rodar; sem `../iodb` tudo roda como hoje
verify_tests: [utest ., utest cacheLedger.t.js]
verify_manual: rodar `utest .` duas vezes seguidas e conferir que quente e frio reportam o MESMO numero de checks com o ledger arbitrando; conferir que `.utest/results.json` continua existindo e atualizado (as duas persistencias em paralelo); dar `touch` num `.t.js` sem editar e conferir que ele NAO re-roda (o ganho sobre o mtime)
verify_confirmed: true
confirmed_at: 2026-09-07
updated: 2026-09-07
files:
  - cache.js
  - cacheLedger.js
  - cacheLedger.t.js
  - ledger.js
  - scanner.js
  - utest.js
---
# [2.7] cache sobre o ledger — o ledger arbitra o frescor por sha256, results.json em paralelo

O ledger passa a responder **"este teste esta fresco?"** — a pergunta que era do
`results.json` — sem desmontar nada do que existe. As duas persistencias rodam **em
paralelo** ate convergirem.

## Objetivo

O `results.json` e o ledger respondem a mesma pergunta com garantias diferentes:

| | `results.json` | ledger |
|---|---|---|
| forma | snapshot mutavel, reescrito por fase | log append-only encadeado |
| ancoragem | `mtime` (o inode) | `sha256` (o conteudo) |
| adulteracao | invisivel | `verify()` localiza (`failedAt`) |
| perda | custa uma rodada fria | custa a historia |

O `mtime` responde *rapido*; o `sha256` responde *certo*. Um `touch` sem edicao derruba o
mtime e forca uma rodada que o conteudo provaria desnecessaria; um `git checkout` que
devolve bytes identicos com mtime novo, idem — trocar de branch e voltar re-rodava a suite
inteira a toa. O ledger arbitra melhor porque sabe **o que o arquivo era**, nao so **quando
o inode mudou**.

A 8.1 fechou dizendo "nenhum dos dois le o outro NESTA feature". Esta e a feature onde eles
passam a se falar, e a direcao da seta e: **cache le ledger**.

## 2.6 foi absorvida aqui

A 2.6 (`results.json` arbitra o cache — segunda checagem sobre o mtime cravado) e a mesma
arbitragem com o outro arbitro. Esta feature montou o ponto de decisao unico (`judge` em
`cache.js`); o caminho `results.json` **e** a 2.6, e passou a ser o **fallback** — o que
roda quando nao ha `../iodb`. Implementar os dois separadamente duplicaria a logica de
promocao so para desmonta-la no sprint seguinte.

## Proxima acao

Feature entregue no sprint 016 e 🟡 testada. A proxima acao e o `sprint eval 2.7` passo a
passo com o humano — em especial o passo do `touch`, que e o unico onde os dois arbitros
divergem por design, e portanto o que prova o ganho.
